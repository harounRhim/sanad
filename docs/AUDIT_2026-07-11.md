# Sanad — Auto-Evaluation (2026-07-11, overnight autonomous pass)

Requested: "analyze the app and see what needs to upgrade and reshape for a
better result … an auto evaluation for a top app that will be used by a lot of
people." Graded against the bar of a consumer app with real traffic, not a
prototype. Each area: score before tonight's pass → after, with what changed
and what still stands between the app and "top app."

Legend: 🟢 ready · 🟡 works, gaps · 🔴 blocker at scale

---

## Scorecard

| Area | Before | After | Notes |
|---|---|---|---|
| Correction engine (backend) | 🟢 | 🟢 | Already the app's crown jewel — validated against real audio, regression-tested, CTC edge crash handled. Untouched tonight. |
| Learning loop (Practice/Listen/Review/SRS) | 🟢 | 🟢 | Complete and coherent: rolling-window grading, 4-stage guided memorization with learner-chosen chunks, SM-2 review. |
| State correctness | 🟡 | 🟢 | **Fixed:** all three progress hooks compounded updates onto render-closure state; burst callers (finishChunk's loop, timers) could read stale state and lose streak increments. Now every mutator reads the live React Query cache. |
| Crash resilience | 🔴 | 🟢 | **Fixed:** no error boundary — any render error white-screened the whole app. Added brand-styled ErrorBoundary (inline styles so it can't be taken down by the thing that crashed). |
| Failure visibility | 🔴 | 🟢 | **Fixed:** if `schema.sql`'s `user_*` tables aren't created, the app looked fully functional while persisting *nothing* (console-only errors). Now a red banner in Layout says exactly what to run. |
| Performance (bundle) | 🔴 | 🟢 | **Fixed:** single 521 kB chunk (Vite's own warning). Now: 16 screens lazy-loaded, app chunk 39 kB, vendors pinned to stable cache chunks (react 165 kB / supabase 212 kB / query 40 kB). Nested Suspense in Layout so the nav shell never flashes away during a lazy load. |
| Account lifecycle | 🔴 | 🟡 | **Fixed the blocker:** mandatory accounts had no password recovery — a forgotten password permanently locked a user out of their progress. Added full reset flow (email → /reset-password → new password). Still 🟡: no email-change or delete-account, and Google OAuth is built but gated off (`VITE_GOOGLE_AUTH_ENABLED`) until the Cloud Console step is possible. |
| First impressions (PWA/meta) | 🔴 | 🟡 | **Fixed:** no favicon, no meta description → added SVG favicon (gold silsila-rings mark on emerald) + description. Still 🟡: no manifest.webmanifest/installability, no offline shell, no og: tags. |
| Loading polish | 🟡 | 🟢 | **Fixed:** Journey flashed every node as not_started before progress loaded; router v7 future-flag warnings. Both gone. |
| Docs honesty | 🟡 | 🟢 | README was three renames stale (screens/hooks/storage story). Rewritten. |

---

## What was verified tonight
- `tsc --noEmit` clean after every change; production build green (23 chunks).
- Live browser: signed-out `/` → `/login` redirect, "Forgot password?" present,
  Google button correctly hidden, `/reset-password` lazy chunk renders with
  the expired-link guard, console clean (router warnings gone).
- **Not verifiable without the user:** signed-in flows (needs a real inbox to
  create an account) and the Supabase `user_*` writes (needs schema.sql run in
  the SQL editor). Both fail visibly now rather than silently.

## Round 2 (same night) — additional fixes shipped
- ~~Tap targets~~ ✅ 28px padded hit areas around the 10px dots (Practice/Review) + aria-labels.
- ~~Practice debug log~~ ✅ gated behind `import.meta.env.DEV`.
- ~~PWA manifest~~ ✅ `manifest.webmanifest` + `icon.svg` + apple-touch-icon (service worker still deliberately deferred).
- ~~Observability~~ ✅ `lib/errorReporting.ts` → `client_errors` table (schema.sql), wired to ErrorBoundary + window.onerror/unhandledrejection, capped 5/page-load.
- ✅ Login redirects already-signed-in visitors instead of re-showing the form.
- ✅ **Loading-flash bug class** from the async-entries migration: ListenRepeat could lock a session onto āyah 1 before progress loaded (real bug); Review/DueForRework flashed "Nothing due 🎉"; SurahDetail flashed a 0% ring. All now wait for `loading`.
- ✅ **Backend regression caught & fixed**: `locate_best_span` preferred superset windows tying a tight match at equal CER (suffix-fit's free gap = zero evidence for the extra āyahs), inflating `ayah_from`. Pruned dominated windows before selection; 56/56 tests pass (pytest itself was missing from `.venv` — reinstalled — which is how the suite went stale unnoticed). **Restart `server/run.ps1` to pick this up.**

## Round 3 (same night)
- ✅ Loading-flash sweep finished: Memorization, Streak screen, Journey's
  header lantern (shows "–" while loading).
- ✅ Settings "Change email" (inline row, confirmation-link flow).

## Backlog to "top app" (priority order, updated)
1. **Run schema.sql (`user_*` + `client_errors` blocks) + create a real
   account** — everything auth-gated is unverified until then (the red
   banner will confirm/deny).
2. **Restart the correction server** to load the localization fix.
3. **Slate consistency (product decision):** ListenRepeat claims a slate slot
   and blocks when full; Practice (reachable from the nav) records progress
   without ever claiming one. Decide one story, then enforce it.
4. **Service worker / offline shell** — manifest shipped; installability
   exists, offline does not yet.
5. **Delete account + data export** (store policy requirement if this ships
   to app stores) — needs a service-role edge function, not client-doable.
6. **i18n:** UI copy is English-only for an app whose audience is
   substantially Arabic/French-speaking. Copy is centralized enough to
   extract.
