# Copie ce fichier en « .env » à la racine du repo, puis renseigne tes valeurs.
# Ne committe JAMAIS le .env réel (ajoute-le à .gitignore).

# URL du projet Supabase (Settings > API > Project URL)
SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co

# Clé service_role pour l'ingestion en masse (Settings > API > service_role).
# ⚠️ Secret : contourne la RLS. À garder côté serveur uniquement, jamais dans le client.
SUPABASE_KEY=eyJhbGciOi...your-service-role-key...

# (optionnel) Token Hugging Face pour Data/download_data.py
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
